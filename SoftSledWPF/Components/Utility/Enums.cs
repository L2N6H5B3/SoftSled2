namespace SoftSledWPF.Components.Utility {
    public enum WMCRenderMode {
        GDI, // GDI Renderer via RDP Console
        RUI  // Remote UI Renderer via RRSP2
    }

    public enum MediaType {
        Video,
        Audio
    }
}
